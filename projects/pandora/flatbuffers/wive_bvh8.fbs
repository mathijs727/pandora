include "contiguous_allocator.fbs";

namespace pandora;

table SerializedWiveBVH8
{
	innerNodeAllocator: SerializedContiguousAllocator;
	leafNodeAllocator: SerializedContiguousAllocator;
	compressedRootHandle: uint32;
	numLeafObjects: uint32;
}

root_type SerializedWiveBVH8;